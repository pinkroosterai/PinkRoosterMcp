POSTGRES_PASSWORD=changeme_dev_only
API_KEY=changeme_dev_only

# MCP server authentication (optional — leave empty for open access)
MCP_API_KEY=

# Dashboard authentication (optional — leave empty for open access)
DASHBOARD_USER=
DASHBOARD_PASSWORD=
